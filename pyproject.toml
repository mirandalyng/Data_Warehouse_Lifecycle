[project]
name = "data-warehouse-lifecycle"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dbt-core>=1.12.5",
    "dbt-snowflake>=1.12.1",
    "dlt[parquet,snowflake]>=1.30.0",
    "ipykernel>=7.3.0",
    "pandas>=3.0.5",
]
