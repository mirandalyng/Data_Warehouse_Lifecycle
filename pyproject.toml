[project]
name = "data-warehouse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dlt[parquet,snowflake]>=1.30.0",
    "ipykernel>=7.3.0",
    "pandas>=3.0.5",
]
